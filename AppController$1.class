package com.documentmanager.model;

import java.time.LocalDateTime;

public record Folder(int folderId, String folderName, int userId, LocalDateTime createdDate) {
    @Override
    public String toString() {
        return folderName;
    }
}
